BACKUP SNAPSHOT of /pipelines as of 2026-06-29, taken right before the copy rewrite
(humanizer + content-creation-engine + Fugu Ultra pass).
This is the single rolling backup. The live, current version lives in /pipelines.
If a future rewrite happens, delete this folder and re-snapshot.
